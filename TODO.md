# TODO

## Canonical Examples

- [ ] Recreate canonical `.domain` example for the distill skill (`src/skills/distill/examples/orders.domain`) — a v3 Orders domain showcasing all constructs: organization/context/module hierarchy, entities with identity/operations/state machines, aggregates, values with invariants, commands, queries, events (internal/external/error/temporal), three service types, policies (reactive), preconditions/postconditions, agreements with reconciliation, interfaces, and `satisfies` traceability.
- [ ] Recreate canonical `.sysreq` example for the distill-sysreq skill — EARS requirements derived from the Orders domain example, demonstrating all six patterns (ubiquitous, state-driven, event-driven, unwanted, optional, complex) with NFRs and traceability.
- [ ] Add canonical examples for each skill that produces output (prd, sysreq, domain) if not already present.
