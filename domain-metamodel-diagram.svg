<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1520 1480" font-family="'Helvetica Neue', Arial, sans-serif">
  <defs>
    <marker id="arr" viewBox="0 0 10 8" refX="10" refY="4" markerWidth="8" markerHeight="6" orient="auto-start-reverse">
      <path d="M0,0 L10,4 L0,8 Z" fill="#E8707A"/>
    </marker>
    <marker id="open-arr" viewBox="0 0 10 8" refX="10" refY="4" markerWidth="9" markerHeight="7" orient="auto-start-reverse">
      <path d="M0,0 L10,4 L0,8" fill="none" stroke="#E8707A" stroke-width="1.4"/>
    </marker>
  </defs>

  <!-- Background -->
  <rect width="1520" height="1480" fill="#E8E8E8" rx="0"/>

  <!-- Outer dashed frame -->
  <rect x="18" y="18" width="1484" height="1444" rx="6" fill="#E8E8E8" stroke="#2C3E50" stroke-width="2" stroke-dasharray="8,4"/>
  <text x="36" y="50" font-size="24" font-weight="700" fill="#2C3E50">Domain</text>

  <!-- ════════════════════════════════════ -->
  <!-- BOXES                                -->
  <!-- ════════════════════════════════════ -->

  <!-- Organization -->
  <rect x="50" y="72" width="190" height="48" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="145" y="102" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Organization</text>

  <!-- Bounded Context -->
  <rect x="50" y="180" width="220" height="48" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="160" y="210" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Bounded Context</text>

  <!-- Module -->
  <rect x="80" y="340" width="160" height="48" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="160" y="370" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Module</text>

  <!-- Interface -->
  <rect x="80" y="450" width="160" height="48" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="160" y="480" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Interface</text>

  <!-- Entity / Aggregate -->
  <rect x="115" y="570" width="200" height="60" rx="4" fill="white" stroke="#2C3E50" stroke-width="2.2"/>
  <text x="215" y="596" text-anchor="middle" font-size="16" font-weight="700" fill="#2C3E50">Entity /</text>
  <text x="215" y="618" text-anchor="middle" font-size="16" font-weight="700" fill="#2C3E50">Aggregate</text>

  <!-- Value Type -->
  <rect x="80" y="920" width="200" height="60" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="180" y="946" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Value Type</text>
  <text x="180" y="966" text-anchor="middle" font-size="11" fill="#555">immutable, transactional ctor</text>

  <!-- Domain Service -->
  <rect x="80" y="1070" width="200" height="55" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="180" y="1095" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Domain Service</text>
  <text x="180" y="1113" text-anchor="middle" font-size="11" fill="#555">cross-entity operations</text>

  <!-- Application Service -->
  <rect x="80" y="1200" width="220" height="48" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="190" y="1230" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Application Service</text>

  <!-- Infrastructure Service -->
  <rect x="80" y="1320" width="240" height="55" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="200" y="1345" text-anchor="middle" font-size="15" font-weight="700" fill="#2C3E50">Infrastructure Service</text>
  <text x="200" y="1363" text-anchor="middle" font-size="11" fill="#555">ACL adapter · 1..1 has Interface</text>

  <!-- ── RIGHT SIDE: Entity children ── -->

  <!-- Operation (entity-owned) -->
  <rect x="520" y="480" width="260" height="72" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="650" y="506" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Operation</text>
  <text x="650" y="524" text-anchor="middle" font-size="10.5" fill="#555">Name, Arguments, Return</text>
  <text x="650" y="538" text-anchor="middle" font-size="10.5" fill="#555">safe|unsafe · idempotent</text>

  <!-- Command -->
  <rect x="520" y="575" width="185" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="613" y="602" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Command</text>

  <!-- Query -->
  <rect x="730" y="575" width="120" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="790" y="602" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Query</text>

  <!-- Event -->
  <rect x="875" y="575" width="120" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="935" y="602" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Event</text>

  <!-- Internal Event -->
  <rect x="520" y="648" width="155" height="36" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.4"/>
  <text x="598" y="671" text-anchor="middle" font-size="12" font-weight="600" fill="#2C3E50">Internal Event</text>

  <!-- Error Event -->
  <rect x="695" y="648" width="135" height="36" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.4"/>
  <text x="763" y="671" text-anchor="middle" font-size="12" font-weight="600" fill="#2C3E50">Error Event</text>

  <!-- External Event -->
  <rect x="850" y="648" width="155" height="36" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.4"/>
  <text x="928" y="671" text-anchor="middle" font-size="12" font-weight="600" fill="#2C3E50">External Event</text>

  <!-- Temporal Event -->
  <rect x="1025" y="648" width="175" height="36" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.4"/>
  <text x="1113" y="671" text-anchor="middle" font-size="12" font-weight="600" fill="#2C3E50">Temporal Event</text>

  <!-- Temporal flavors annotation -->
  <text x="1113" y="700" text-anchor="middle" font-size="9.5" fill="#777" font-style="italic">relative | absolute | recurring</text>
  <text x="1113" y="713" text-anchor="middle" font-size="9.5" fill="#777" font-style="italic">guard suppresses at firing time</text>

  <!-- State Machine -->
  <rect x="520" y="740" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="620" y="767" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">State Machine</text>

  <!-- State -->
  <rect x="790" y="740" width="120" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="850" y="767" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">State</text>

  <!-- Transition -->
  <rect x="980" y="735" width="145" height="55" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="1053" y="758" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Transition</text>
  <text x="1053" y="776" text-anchor="middle" font-size="10.5" fill="#555">triggered by Operation</text>

  <!-- Repository -->
  <rect x="520" y="820" width="260" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="650" y="847" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Repository</text>

  <!-- Invariant -->
  <rect x="520" y="895" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="620" y="922" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Invariant</text>

  <!-- Policy -->
  <rect x="750" y="895" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="850" y="922" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Policy</text>

  <!-- Agreement -->
  <rect x="520" y="970" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="620" y="997" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Agreement</text>

  <!-- Reconciliation -->
  <rect x="750" y="970" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="850" y="997" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Reconciliation</text>

  <!-- Escalation Chain -->
  <rect x="750" y="1045" width="200" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="850" y="1072" text-anchor="middle" font-size="13" font-weight="700" fill="#2C3E50">Escalation Chain</text>

  <!-- Value Type children (right side) -->

  <!-- VT Operation -->
  <rect x="520" y="1070" width="260" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="650" y="1097" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Operation</text>

  <!-- Domain Service Operation (right side) -->
  <rect x="520" y="1200" width="260" height="44" rx="4" fill="white" stroke="#2C3E50" stroke-width="1.8"/>
  <text x="650" y="1227" text-anchor="middle" font-size="14" font-weight="700" fill="#2C3E50">Operation</text>

  <!-- ════════════════════════════════════ -->
  <!-- RELATIONSHIP LINES (coral/salmon)    -->
  <!-- ════════════════════════════════════ -->

  <!-- 1. Organization → Bounded Context -->
  <path d="M 100,120 L 100,180" fill="none" stroke="#E8707A" stroke-width="1.6" marker-end="url(#arr)"/>
  <text x="60" y="155" font-size="11" fill="#E8707A" font-weight="500">1..n</text>

  <!-- 2. BC self-loop (0..n context map) -->
  <path d="M 270,195 C 320,170 320,230 270,215" fill="none" stroke="#E8707A" stroke-width="1.4" marker-end="url(#arr)"/>
  <text x="328" y="195" font-size="10" fill="#E8707A" font-weight="500">0..n</text>
  <text x="328" y="208" font-size="9" fill="#E8707A" font-style="italic">context map</text>

  <!-- 3. BC → Module -->
  <path d="M 120,228 L 120,340" fill="none" stroke="#E8707A" stroke-width="1.6" marker-end="url(#arr)"/>
  <text x="80" y="290" font-size="11" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 4. Module self-loop (depends on) -->
  <path d="M 80,352 C 40,330 40,385 80,375" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="25" y="350" font-size="9" fill="#E8707A" font-weight="500" text-anchor="end">0..n</text>
  <text x="25" y="363" font-size="8" fill="#E8707A" font-style="italic" text-anchor="end">depends on</text>

  <!-- 5. Module → Interface (0..n exposes) -->
  <path d="M 160,388 L 160,450" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="170" y="425" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 6. Module → Entity/Aggregate (0..n) -->
  <path d="M 160,388 Q 180,470 215,570" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="150" y="510" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 7. Interface → Entity exposes from (dashed) -->
  <path d="M 200,498 Q 260,540 260,570" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="4,3" marker-end="url(#arr)"/>
  <text x="260" y="545" font-size="9" fill="#E8707A" font-style="italic">exposes from</text>

  <!-- ── Entity/Aggregate right-side relations ── -->

  <!-- 8. Entity → Operation (0..n owns) -->
  <path d="M 315,580 L 520,516" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="430" y="536" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 9. Entity → Command (0..n) -->
  <path d="M 315,590 L 520,595" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>

  <!-- 10. Entity → Event (0..n) -->
  <path d="M 315,600 Q 600,600 875,595" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>

  <!-- 11. Entity → State Machine (0..n) -->
  <path d="M 315,610 Q 430,680 520,755" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="400" y="690" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 12. Entity → Repository (0..1 derives) -->
  <path d="M 315,618 Q 440,740 520,840" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="395" y="760" font-size="10" fill="#E8707A" font-weight="500">0..1</text>

  <!-- 13. Entity → Invariant (0..n) -->
  <path d="M 315,625 Q 440,800 520,910" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="418" y="820" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 14. Entity → Agreement (0..n) -->
  <path d="M 315,628 Q 450,850 520,985" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="425" y="870" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 15. Entity self-loop (relates to) -->
  <path d="M 155,570 C 130,540 295,540 280,570" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="215" y="543" text-anchor="middle" font-size="9" fill="#E8707A" font-weight="500">0..n relates to</text>

  <!-- 16. Entity → Value Type (0..n embeds) -->
  <path d="M 180,630 L 180,920" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="135" y="780" font-size="10" fill="#E8707A" font-weight="500">0..n</text>
  <text x="135" y="795" font-size="9" fill="#E8707A" font-style="italic">embeds</text>

  <!-- 17. Entity → Domain Service (0..n) -->
  <path d="M 145,630 L 145,1070" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="100" y="1010" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- ── State Machine details ── -->

  <!-- 18. State Machine → State (1..n has) -->
  <path d="M 720,762 L 790,762" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="745" y="755" font-size="10" fill="#E8707A" font-weight="500">1..n</text>

  <!-- 19. State → Transition (self-loop / link) -->
  <path d="M 910,755 L 980,755" fill="none" stroke="#E8707A" stroke-width="1.4" marker-end="url(#arr)"/>

  <!-- 20. State self-loop (invariants) -->
  <path d="M 850,784 C 830,810 870,810 850,784" fill="none" stroke="#E8707A" stroke-width="1.2"/>
  <text x="850" y="825" text-anchor="middle" font-size="8.5" fill="#E8707A" font-style="italic">invariants</text>

  <!-- ── Event subtypes (is-a, dashed) ── -->

  <!-- 21. Event → Internal Event -->
  <path d="M 910,619 L 630,648" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="5,3" marker-end="url(#open-arr)"/>

  <!-- 22. Event → Error Event -->
  <path d="M 925,619 L 770,648" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="5,3" marker-end="url(#open-arr)"/>

  <!-- 23. Event → External Event -->
  <path d="M 945,619 L 935,648" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="5,3" marker-end="url(#open-arr)"/>

  <!-- 24. Event → Temporal Event -->
  <path d="M 960,619 L 1090,648" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="5,3" marker-end="url(#open-arr)"/>

  <!-- ── Command/Query/Event details ── -->

  <!-- 25. Command → Operation (triggers) -->
  <path d="M 613,575 L 630,552" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="570" y="560" font-size="9" fill="#E8707A" font-style="italic">triggers</text>

  <!-- 26. Operation → Event (emits) -->
  <path d="M 780,516 Q 850,545 890,575" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="840" y="538" font-size="9" fill="#E8707A" font-style="italic">0..n emits</text>

  <!-- 27. Query → Repository (reads from) -->
  <path d="M 790,619 Q 710,720 660,820" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="688" y="720" font-size="9" fill="#E8707A" font-style="italic">1..1 reads from</text>

  <!-- 28. External Event → Command (triggers) -->
  <path d="M 870,684 Q 700,720 613,619" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="740" y="708" font-size="9" fill="#E8707A" font-weight="500">1..n triggers</text>

  <!-- ── Policy reactive loop ── -->

  <!-- 29. Internal Event → Policy (triggers) -->
  <path d="M 598,684 Q 700,790 780,895" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="660" y="810" font-size="9" fill="#E8707A" font-weight="500">triggers</text>

  <!-- 30. Error Event → Policy (triggers) -->
  <path d="M 763,684 Q 800,790 840,895" fill="none" stroke="#E8707A" stroke-width="1.2" marker-end="url(#arr)"/>

  <!-- 31. Temporal Event → Policy (triggers) -->
  <path d="M 1080,684 Q 980,790 880,895" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="1000" y="800" font-size="9" fill="#E8707A" font-weight="500">triggers</text>

  <!-- 32. Policy → Command (effect) -->
  <path d="M 850,939 Q 900,960 920,945 Q 960,920 920,880 Q 850,820 700,720 Q 640,680 613,619" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="940" y="875" font-size="9" fill="#E8707A" font-weight="500">effect</text>

  <!-- ── Agreement / Reconciliation ── -->

  <!-- 33. Agreement → Reconciliation (1..1 maintained by) -->
  <path d="M 720,992 L 750,992" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="705" y="985" font-size="8.5" fill="#E8707A" font-weight="500">1..1</text>

  <!-- 34. Reconciliation → Escalation Chain (0..1) -->
  <path d="M 850,1014 L 850,1045" fill="none" stroke="#E8707A" stroke-width="1.4" marker-end="url(#arr)"/>
  <text x="860" y="1036" font-size="9" fill="#E8707A" font-weight="500">0..1</text>

  <!-- 35. Reconciliation → Event (listens to) -->
  <path d="M 950,980 Q 1050,840 970,619" fill="none" stroke="#E8707A" stroke-width="1.1" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="1020" y="815" font-size="8.5" fill="#E8707A" font-style="italic">0..n listens to</text>

  <!-- 36. Reconciliation → Command (issues) -->
  <path d="M 810,970 Q 700,870 613,619" fill="none" stroke="#E8707A" stroke-width="1.1" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="710" y="870" font-size="8.5" fill="#E8707A" font-style="italic">1..n issues</text>

  <!-- ── Value Type children ── -->

  <!-- 37. Value Type → Operation (0..n) -->
  <path d="M 280,1092 L 520,1092" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="400" y="1085" font-size="10" fill="#E8707A" font-weight="500">0..n</text>

  <!-- 38. Value Type self-loop (composes) -->
  <path d="M 120,920 C 90,890 60,940 80,950" fill="none" stroke="#E8707A" stroke-width="1.2" marker-end="url(#arr)"/>
  <text x="42" y="920" font-size="9" fill="#E8707A" font-weight="500">0..n</text>
  <text x="32" y="933" font-size="8" fill="#E8707A" font-style="italic">composes</text>

  <!-- ── Domain Service children ── -->

  <!-- 39. Domain Service → Operation (1..n) -->
  <path d="M 280,1097 Q 400,1160 520,1222" fill="none" stroke="#E8707A" stroke-width="1.5" marker-end="url(#arr)"/>
  <text x="370" y="1175" font-size="10" fill="#E8707A" font-weight="500">1..n</text>

  <!-- 40. Domain Service → Infra Service (calls) -->
  <path d="M 160,1125 L 160,1320" fill="none" stroke="#E8707A" stroke-width="1.2" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="110" y="1240" font-size="9" fill="#E8707A" font-style="italic">0..n calls</text>

  <!-- 41. App Service → Domain Service (delegates) -->
  <path d="M 190,1200 L 180,1125" fill="none" stroke="#E8707A" stroke-width="1.3" marker-end="url(#arr)"/>
  <text x="220" y="1170" font-size="9" fill="#E8707A" font-style="italic">delegates</text>

  <!-- 42. App Service → Entity (delegates) -->
  <path d="M 250,1200 Q 350,900 260,630" fill="none" stroke="#E8707A" stroke-width="1.1" stroke-dasharray="3,2" marker-end="url(#arr)"/>
  <text x="340" y="910" font-size="9" fill="#E8707A" font-style="italic">delegates</text>

  <!-- 43. Interface → Domain Service (exposes from, dashed) -->
  <path d="M 140,498 Q 100,800 120,1070" fill="none" stroke="#E8707A" stroke-width="1.1" stroke-dasharray="4,3" marker-end="url(#arr)"/>
  <text x="78" y="700" font-size="9" fill="#E8707A" font-style="italic">exposes from</text>

  <!-- 44. Interface → Operation (exposes) -->
  <path d="M 240,474 L 520,504" fill="none" stroke="#E8707A" stroke-width="1.4" marker-end="url(#arr)"/>
  <text x="370" y="480" font-size="10" fill="#E8707A" font-weight="500">1..n exposes</text>

  <!-- 45. Entity → Infra Service (uses via interface, dashed) -->
  <path d="M 115,625 Q 60,980 120,1320" fill="none" stroke="#E8707A" stroke-width="1.0" stroke-dasharray="3,3" marker-end="url(#arr)"/>
  <text x="42" y="980" font-size="8.5" fill="#E8707A" font-style="italic">uses</text>
  <text x="42" y="993" font-size="8.5" fill="#E8707A" font-style="italic">(via iface)</text>

  <!-- ════════════════════════════════════ -->
  <!-- CARDINALITY LABELS on right side     -->
  <!-- ════════════════════════════════════ -->

  <!-- Entity right-side cardinalities (stacked labels) -->
  <text x="500" y="598" font-size="10" fill="#E8707A" font-weight="500" text-anchor="end">0..n</text>
  <text x="500" y="768" font-size="10" fill="#E8707A" font-weight="500" text-anchor="end">0..n</text>
  <text x="500" y="845" font-size="10" fill="#E8707A" font-weight="500" text-anchor="end">0..1</text>

  <!-- ════════════════════════════════════ -->
  <!-- REACTIVE LOOP ANNOTATION             -->
  <!-- ════════════════════════════════════ -->
  <rect x="505" y="460" width="720" height="650" rx="10" fill="none" stroke="#CCC" stroke-width="1" stroke-dasharray="6,4"/>
  <text x="865" y="1098" text-anchor="middle" font-size="10" fill="#AAA" font-style="italic">Reactive loop: Command → Operation → Event → Policy → Command</text>

  <!-- Query annotation -->
  <text x="790" y="630" text-anchor="middle" font-size="9" fill="#777" font-style="italic">safe, idempotent</text>

</svg>